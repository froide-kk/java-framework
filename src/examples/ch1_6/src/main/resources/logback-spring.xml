<configuration>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <charset>UTF-8</charset>
            <pattern>%d{yyyy/MM/dd HH:mm:ss} %-5level [%thread] - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="FOO-FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- 日毎にログファイルを切り替える -->
            <fileNamePattern>foo-%d{yyyy-MM-dd}.txt</fileNamePattern>
            <!-- 保持しておくログアーカイブファイルの最大数 -->
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%date %level [%thread] %logger{10} [%file : %line] %msg%n</pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="STDOUT" />
    </root>

    <logger name="jp.co.froide.javaframework.controllers.HelloController" level="ERROR">
        <!-- 定義した FOO-FILE アペンダーに出力 -->
        <appender-ref ref="FOO-FILE"/>
    </logger>
</configuration>